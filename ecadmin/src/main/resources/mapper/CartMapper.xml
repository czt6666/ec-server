<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bistu.ecadmin.dao.mapper.CartMapper">

    <resultMap id="CartMap" type="com.bistu.ecadmin.pojo.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="sku_id" property="skuId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 幂等加入购物车：若存在则触达更新时间 -->
    <insert id="insertOrTouch">
        insert into cart (user_id, sku_id)
        values (#{userId}, #{skuId})
            on duplicate key update update_time = current_timestamp
    </insert>

    <!-- 查询某用户购物车列表 -->
    <select id="selectByUserId" parameterType="java.lang.Integer" resultMap="CartMap">
        select id, user_id, sku_id, create_time, update_time
        from cart
        where user_id = #{userId}
        order by update_time desc, create_time desc
    </select>

    <!-- 删除购物车中的某个 SKU -->
    <delete id="deleteByUserAndSku">
        delete from cart
        where user_id = #{userId}
          and sku_id = #{skuId}
    </delete>
    <select id="selectByUserIdPaged" resultMap="CartMap">
        select id, user_id, sku_id, create_time, update_time
        from cart
        where user_id = #{userId}
        order by update_time desc, create_time desc
            limit #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" parameterType="int" resultType="int">
        select count(1) from cart where user_id = #{userId}
    </select>
    <select id="selectAllPaged" resultMap="CartMap">
        select id, user_id, sku_id, create_time, update_time
        from cart
        order by update_time desc, create_time desc
            limit #{offset}, #{pageSize}
    </select>

    <select id="countAll" resultType="int">
        select count(1) from cart
    </select>
</mapper>
